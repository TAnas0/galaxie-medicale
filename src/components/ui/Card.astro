---
const { class: className, style } = Astro.props;
---

<div
  class={`relative rounded-lg bg-white dark:bg-muted-900 transition-all duration-300 ${className ?? ''}`}
  style={style}
>
  <slot />
</div>

---
// import Button from './ui/Button.astro';
import { Button } from 'free-astro-components';
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components'
import xray1Image from '../assets/hero/xray1.webp'
import surgery1Image from "../assets/hero/surgery1.webp";
import nurse1Image from "../assets/hero/nurse1.webp";
import medicalImage from "../assets/hero/medical-room.webp";
import microscopeImage from "../assets/hero/microscope.webp";
import nurseImage from "../assets/hero/nurse-patient.webp";
import pillsImage from "../assets/hero/pills.webp";
// import ArrowRight from '@iconify-icons/heroicons-outline/arrow-right'; // or use any icon source
// import { Icon } from '@iconify-icon/astro'; // use if you're using Iconify
---
<section
  class="relative px-4 pt-32 pb-10 sm:px-6 lg:px-8"
>
  <div class="pattern-layer"></div>

  <!-- Floating circular images -->
  <!-- <div id="floating-images" class="inset-0 z-10 pointer-events-none opacity-90">
    <div class="fade-in absolute top-[65%] right-8 sm:right-12 md:right-20">
      <Image src={nurse1Image} class="fade-in max-w-64 max-h-64 rounded-full object-cover shadow-2xl" alt="Medical equipment" fetchpriority="high">
    </div>
    <div class="fade-in absolute top-[12%] left-18 sm:left-22 md:left-32">
      <Image src={medicalImage} class="opacity-0 fade-in max-w-sm max-h-sm rounded-full object-cover shadow-2xl z-2 bg-blue-100" alt="Medical equipment">
    </div>
    <div class="fade-in absolute top-[55%] left-[10%] sm:right-48 md:right-[56%]">
      <Image src={microscopeImage} class="opacity-0 fade-in max-w-64 max-h-64 rounded-full object-cover shadow-2xl z-3 bg-blue-100" alt="Medical equipment">
    </div>
    <div class="fade-in absolute top-[55%] left-25 sm:right-8 md:right-12">
      <Image src={nurseImage} class="opacity-0 fade-in max-w-32 max-h-32 rounded-full object-cover shadow-2xl z-3 bg-blue-100" alt="Medical equipment">
    </div>
    <div class="fade-in absolute top-[20%] right-6 sm:right-0 md:right-96">
      <Image src={surgery1Image} class="opacity-0 fade-in absolute max-w-sm max-h-sm rounded-full object-cover shadow-2xl bg-blue-100" alt="Medical equipment">
    </div>
  </div> -->

  <div id="hero-content" class="max-w-7xl mx-auto z-10 relative">
    <div class="text-center">
      <!-- <h1
        class="text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 mb-8 leading-tight"
      >
        Galaxie Médicale
      </h1> -->

      <p
        class="text-4xl sm:text-2xl text-gray-600 mb-16 max-w-4xl mx-auto leading-relaxed mt-32 bg-white/80 text-black px-4 py-2 rounded"
      >
        Dispositifs médicaux performants pour les professionnels de santé.
        <br>
        Fiables, précis, adaptés au diagnostic, au traitement et à l’analyse.
      </p>



    <div class="mt-8 relative">
      <div
        class="bg-testpink backdrop-blur-sm rounded-2xl p-8 shadow-xl border border-gray-200 max-w-4xl mx-auto"
      >
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
          <div>
            <div class="text-3xl font-bold text-blue-600 mb-2">+10</div>
            <div class="text-gray-600">ans d’expérience</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-blue-600 mb-2">+100</div>
            <div class="text-gray-600">Partenaires Médicaux</div>
          </div>
          <div>
            <div class="text-3xl font-bold text-blue-600 mb-2">+500</div>
            <div class="text-gray-600">Produits Médicaux</div>
          </div>
        </div>
      </div>
    </div>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mt-12">
      <Button
        href="/galaxie-medicale/catalog"
        size="lg"
        label="Consultez notre catalogue..."
        icon='arrow-right' iconPosition='right'
        class="text-white px-8 py-3 text-lg font-semibold rounded-lg transition-all duration-500 transform hover:scale-105 shadow-lg"
      >
        Voir le catalogue
      </Button>

      <!-- <Button
        variant="outline"
        size="lg"
        label="Contact Us"
        class="border-2 border-red-600 text-red-600 hover:bg-red-600 hover:text-white px-8 py-3 text-lg font-semibold rounded-lg transition-all duration-500"
      >
        Contact Us
      </Button> -->
    </div>
  </div>
  </div>
</section>


<style>
  .pattern-layer {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background-image:
      linear-gradient(0deg, transparent 24%, rgba(0,0,0,0.03) 25%, rgba(0,0,0,0.03) 26%, transparent 27%, transparent 74%, rgba(0,0,0,0.03) 75%, rgba(0,0,0,0.03) 76%, transparent 77%, transparent),
      linear-gradient(90deg, transparent 24%, rgba(0,0,0,0.03) 25%, rgba(0,0,0,0.03) 26%, transparent 27%, transparent 74%, rgba(0,0,0,0.03) 75%, rgba(0,0,0,0.03) 76%, transparent 77%, transparent);
    background-size: 50px 50px;
    background-repeat: repeat;
    z-index: -1;
    pointer-events: none;
  }
    
  .fade-in {
    animation: fade-in 4s ease-out forwards;
  }

  img {
    color: transparent; /* Makes alt text invisible if shown */
    text-indent: 100%;  /* Pushes text off-screen */
    white-space: nowrap;
    overflow: hidden;
  }
</style>

<script is:client>
  window.addEventListener('load', () => {
    document.querySelectorAll('#floating-images img').forEach(img => {
      console.log("Found image");
      console.log(img.complete);
      if (img.complete && img.naturalHeight !== 0) {
        console.log("Image ready");
        img.parentElement.classList.remove('hidden');
        img.parentElement.classList.add('block');
      } else {
        img.addEventListener('load', () => {
          console.log("Image load event fired", img);
          img.parentElement.classList.remove('hidden');
          img.parentElement.classList.add('block');
        });
        img.addEventListener('error', () => {
          console.log("Image failed to load", img);
        });
      }
    });
  });
  </script>
  